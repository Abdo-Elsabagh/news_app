import 'package:flutter/material.dart';

class CategroyViem extends StatelessWidget {
  const CategroyViem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: SizedBox(),
    );
  }
}
